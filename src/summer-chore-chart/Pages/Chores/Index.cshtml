@page
@model summer_chore_chart.Pages.Chores.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Chorelist[0].Chorename)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Chorelist[0].Choreamt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Chorelist) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Chorename)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Choreamt)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
